{
  "name": "server",
  "version": "1.0.0",
  "type": "module",
  "author": "Archie Newman",
  "description": "server for managing todos",
  "license": "ISC",
  "main": "index.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf build",
    "clean-all": "rm -rf node_modules build",
    "start": "npm run build && node build/index.js"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "fastify": "^5.4.0"
  }
}
